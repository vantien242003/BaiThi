<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Create Product</title>

  <!-- Thêm Bootstrap CSS -->
  <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css" rel="stylesheet">

  <!-- Thêm CSS Tùy Chỉnh -->
  <style>
    .form-container {
      background-color: #f8f9fa;
      padding: 30px;
      border-radius: 8px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
    }

    .form-container h2 {
      margin-bottom: 30px;
      font-size: 2rem;
      font-weight: bold;
    }

    .form-group label {
      font-weight: bold;
    }

    .btn-primary {
      padding: 10px 20px;
    }
  </style>
</head>
<body>
  <div class="container mt-5">
    <div class="form-container">
      <h2>Create Product</h2>

      <%= form_with model: product, class: "product-form" do |form| %>
        <div class="form-group">
          <%= form.label :name, class: "control-label" %>
          <%= form.text_field :name, class: "form-control", placeholder: "Enter product name" %>
        </div>

        <div class="form-group">
          <%= form.label :description, class: "control-label" %>
          <%= form.rich_text_area :description, class: "form-control", placeholder: "Enter product description" %>
        </div>

        <div class="form-group">
          <%= form.label :featured_image, class: "control-label" %>
          <%= form.file_field :featured_image, accept: "image/*", class: "form-control-file" %>
        </div>

        <div class="form-group">
          <%= form.label :inventory_count, class: "control-label" %>
          <%= form.number_field :inventory_count, class: "form-control", placeholder: "Enter inventory count" %>
        </div>

        <div class="form-group">
          <%= form.submit "Create Product", class: "btn btn-primary" %>
        </div>
      <% end %>
    </div>
  </div>

  <!-- Thêm Bootstrap JS (nếu cần) -->
  <script src="https://code.jquery.com/jquery-3.3.1.slim.min.js"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.7/umd/popper.min.js"></script>
  <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js"></script>
</body>
</html>
